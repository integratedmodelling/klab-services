package org.integratedmodelling.klab.runtime.scale.space;

public class Snippets {


// Suggestion for WFS support (can be adapted for parsing other things)
//    HttpServletRequest request=...;
//    org.geotools.xml.parser.Parser parser = new Parser(new org.geotools.wfs.v1_1.WFSConfiguration());
//    TransactionType tt = (TransactionType) parser.parse(request.getReader());
//    InsertElementType insert1 = (InsertElementType) tt.getInsert().get(0);


}
