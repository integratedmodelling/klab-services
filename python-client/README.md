# k.LAB Python client scaffold

This package contains Python abstractions and client-side implementation skeletons mirroring key
interfaces in `klab.core.api` and their reference client-side implementation style from
`klab.core.common`.
