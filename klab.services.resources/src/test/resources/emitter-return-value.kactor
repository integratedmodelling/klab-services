behavior test.emitter.return.value
    "An emitter whose reactive return supplies an exit code."
    version 1.0
    using
        core.timer as timer

action main:
    timer.random(step=10.s):
        time -> return time
    fire "started"
